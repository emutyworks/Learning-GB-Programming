SoundDataTbl:
; $Cx empty: x = $1-$f
; $Dx octave: x = $0-$5 (2-7)
; $FF end of data
;
; $xx
;  |+ note: $0-$b (C,C#,D,D#,E,F,F#,G,G#,A,A#,B)
;  + volume: $0-$f
;
; Total {{TOTAL_DATA}} bytes of data, Uses {{USE_WRAM}} bytes of WRAM
{{DATA}}